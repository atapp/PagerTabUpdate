import XCTest
@testable import TabStripWork5

final class TabStripWork5Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TabStripWork5().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
